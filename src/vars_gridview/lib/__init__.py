"""Core library package for VARS GridView.

The package is organised by responsibility:

- ``annotation``: observation/association domain objects and annotation UI glue.
- ``auth``: authentication helpers (Raziel).
- ``common``: generic non-UI helpers (time/path/TSV parsing).
- ``config``: application constants and persisted settings.
- ``runtime``: logging and Qt worker/runable primitives.
- ``sorting``: ROI/mosaic sort strategies.
- ``vision``: image and embedding helpers.
"""
